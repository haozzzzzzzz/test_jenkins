pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'go version'
                sh 'echo "hello, world"'
                sh 'pwd'
            }
        }
    }
}
